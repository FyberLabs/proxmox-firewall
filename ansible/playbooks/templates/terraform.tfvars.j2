# Template for {{ inventory_hostname }}
proxmox_api_secret = "{{ lookup('env', 'PROXMOX_API_SECRET_' + location_prefix | upper + '_PROXMOX') }}"
proxmox_host = "{{ proxmox_mgmt_ip | regex_replace('/.*', '') }}"
network_prefix = "{{ network_prefix }}"
ssh_public_key = "{{ lookup('file', playbook_dir + '/../credentials/' + inventory_hostname + '_root.pub') }}"
tailscale_auth_key = "{{ lookup('env', 'TF_VAR_tailscale_auth_key') }}"
tailscale_password = "{{ lookup('env', 'TF_VAR_tailscale_password') }}"
omada_password = "{{ lookup('env', 'TF_VAR_omada_password') }}"
timezone = "{{ timezone }}"
target_node = "{{ lookup('env', 'TF_VAR_target_node') }}"
ssh_private_key_file = "{{ lookup('env', 'ANSIBLE_SSH_PRIVATE_KEY_FILE') }}"
